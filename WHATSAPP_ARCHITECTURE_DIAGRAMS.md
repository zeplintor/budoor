# WhatsApp Scheduling - Architecture Diagrams

## 1. System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE (Browser)                     │
│                   /dashboard/settings                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ WhatsAppScheduleManager.tsx                                 │  │
│  │  - Create/Edit/Delete schedules                            │  │
│  │  - Toggle active/inactive                                  │  │
│  │  - Show list with next send times                          │  │
│  └──────────────────┬───────────────────────────────────────────┘  │
│                     │                                                │
└─────────────────────┼────────────────────────────────────────────────┘
                      │ Firestore API Calls
                      │
        ┌─────────────▼────────────────────┐
        │  src/lib/firebase/              │
        │  whatsappSchedules.ts           │
        │  CRUD Operations:               │
        │  - create, read, update, delete │
        │  - Toggle active status         │
        └─────────────┬────────────────────┘
                      │
        ┌─────────────▼────────────────────────────────────────┐
        │         Firebase Firestore                          │
        │  Collection: users/{userId}/whatsappSchedules       │
        │                                                      │
        │  Document fields:                                  │
        │  - Scheduling info (frequency, time, timezone)     │
        │  - Content options (audio, charts, message)        │
        │  - Metadata (created, lastSent, nextSend, count)   │
        └─────────────┬────────────────────────────────────────┘
                      │ Firestore onChange trigger
                      │
        ┌─────────────▼────────────────────────────────────────┐
        │  initializeScheduleNextSendTime                     │
        │  Cloud Function (onCreate trigger)                  │
        │  - Calculate initial nextSendAt                     │
        │  - Update Firestore document                        │
        └────────────────────────────────────────────────────────┘
```

---

## 2. Scheduled Delivery Flow

```
┌────────────────────────────────────────────────────────────┐
│          Google Cloud Scheduler                             │
│                                                              │
│  Job: "send-whatsapp-reports"                              │
│  Schedule: "*/15 * * * *" (every 15 minutes)              │
│  Timezone: "Africa/Casablanca"                             │
└────────────────┬───────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────┐
│          Google Pub/Sub                                     │
│                                                              │
│  Topic: "send-whatsapp-reports"                            │
│  Publishes message: {}                                     │
└────────────────┬───────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────┐
│    sendScheduledWhatsAppReports()                           │
│    Cloud Function (Pub/Sub trigger)                        │
│                                                              │
│    1. Query all users with whatsappSchedules               │
│    2. For each schedule:                                   │
│       - Check if isActive === true                         │
│       - Check if now >= nextSendAt                         │
│       - Check if now < nextSendAt + 15 minutes             │
│    3. For matching schedules:                              │
│       - Fetch latest report for field (parcelleId)         │
│       - Call sendWhatsAppReport()                          │
│    4. Update Firestore:                                    │
│       - Set lastSentAt = now                               │
│       - Increment sendCount                                │
│       - Calculate new nextSendAt                           │
└────────────────┬───────────────────────────────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
        ▼                 ▼
┌──────────────────┐  ┌──────────────────────────────┐
│ Firebase Admin   │  │ sendWhatsAppReport()         │
│ Query reports    │  │ Service function             │
│ collection       │  │                              │
└──────────────────┘  │ 1. Format message            │
                      │ 2. Add recommendations       │
                      │ 3. Add audio link            │
                      │ 4. Add Darija script         │
                      │ 5. Call Twilio API           │
                      └──────────┬───────────────────┘
                                 │
                                 ▼
                      ┌──────────────────────────┐
                      │  Twilio WhatsApp API     │
                      │                          │
                      │  sends message via:      │
                      │  Client.messages.create()│
                      └──────────┬───────────────┘
                                 │
                                 ▼
                      ┌──────────────────────────┐
                      │  User's WhatsApp         │
                      │  📱 Message Received     │
                      └──────────────────────────┘
```

---

## 3. Message Content Flow

```
┌──────────────────────────────────────────────────────────────┐
│  sendWhatsAppReport(phoneNumber, report, schedule, ...)    │
└──────────────────┬───────────────────────────────────────────┘
                   │
        ┌──────────┼──────────┬────────────┐
        │          │          │            │
        ▼          ▼          ▼            ▼
    ┌────────┐ ┌──────────┐ ┌─────────┐ ┌──────────┐
    │ Report │ │ Schedule │ │ Audio   │ │ Darija   │
    │ Object │ │ Config   │ │ URL     │ │ Script   │
    └───┬────┘ └────┬─────┘ └────┬────┘ └─────┬───┘
        │           │            │           │
        └───────────┼────────────┼───────────┘
                    │            │
                    ▼            ▼
        ┌─────────────────────────────────┐
        │  Format WhatsApp Message:       │
        │                                 │
        │  📊 [Field Name] Report         │
        │  Status: ✅/⚠️/🔴              │
        │                                 │
        │  🎯 Recommendations:            │
        │  • [Top 3 recommendations]      │
        │                                 │
        │  🎤 Darija Script:              │
        │  [First 200 chars excerpt]      │
        │                                 │
        │  🎵 Audio: [Link to MP3]        │
        │                                 │
        │  Budoor 🌾                      │
        └──────────────┬──────────────────┘
                       │
                       ▼
        ┌────────────────────────────────┐
        │  Twilio WhatsApp API           │
        │  .messages.create({            │
        │    from: TWILIO_WHATSAPP_PHONE │
        │    to: userPhoneNumber         │
        │    body: formattedMessage      │
        │    mediaUrl: audioUrl          │
        │  })                            │
        └────────────┬───────────────────┘
                     │
                     ▼
        ┌────────────────────────────────┐
        │  ✅ Message Sent               │
        │  (or error logged)             │
        └────────────────────────────────┘
```

---

## 4. Data Model Relationships

```
┌─────────────────────────────────────────────────────────┐
│  Firestore Collections & References                     │
└─────────────────────────────────────────────────────────┘

users/{userId}
├── id (string)
├── email (string)
├── phoneNumber (string) ◄─────────────────────┐
├── subscriptionStatus (string)                │
└── whatsappSchedules/ (subcollection)         │
    └── {scheduleId}                           │
        ├── id (string)                        │
        ├── userId (string) ◄─ Reference      │
        ├── parcelleId (string) ──────────────┐
        ├── parcelleName (string)              │
        ├── isActive (boolean)                 │
        ├── frequency (string)                 │
        ├── daysOfWeek (array)                 │
        ├── dayOfMonth (number)                │
        ├── time (string, "HH:mm")             │
        ├── timezone (string)                  │
        ├── includeAudio (boolean)             │
        ├── includeChart (boolean)             │
        ├── customMessage (string)             │
        ├── createdAt (timestamp)              │
        ├── updatedAt (timestamp)              │
        ├── lastSentAt (timestamp)             │
        ├── nextSendAt (timestamp)             │
        └── sendCount (number)                 │
                                               │
parcelles/{parcelleId} ◄─────────────────────┘
├── id (string)
├── userId (string)
├── name (string)
├── location (geolocation)
└── ...

reports/{reportId}
├── id (string)
├── userId (string)
├── parcelleId (string) ◄─ Used when generating latest report
├── status (string)
├── recommendations (array)
├── audioUrl (string)
├── darijaScript (string)
└── ...
```

---

## 5. Time Calculation Logic

```
┌──────────────────────────────────────────────────────┐
│  calculateNextSendTime(schedule)                     │
└──────────────────┬───────────────────────────────────┘
                   │
        ┌──────────▼──────────┐
        │ Parse schedule.time │
        │ In user timezone    │
        └──────────┬──────────┘
                   │
            ┌──────▼──────────────────────────┐
            │ Check frequency type            │
            └──────┬─────────────────────────┬┘
                   │                         │
       ┌───────────┴──────────┐              │
       │                      │              │
       ▼                      ▼              ▼
   ┌────────┐          ┌──────────┐      ┌─────────┐
   │ DAILY  │          │ WEEKLY   │      │MONTHLY  │
   └────┬───┘          └────┬─────┘      └────┬────┘
        │                   │                 │
        ▼                   ▼                 ▼
   Add 24h        Check daysOfWeek      Set dayOfMonth
   to today       Find next occurrence  Find next month
        │                   │                 │
        └───────────────────┴─────────────────┘
                            │
                            ▼
                  ┌──────────────────────┐
                  │ Set time to HH:mm    │
                  │ In user timezone     │
                  └──────────┬───────────┘
                             │
                             ▼
                  ┌──────────────────────┐
                  │ Convert to UTC       │
                  │ (Timestamp)          │
                  └──────────┬───────────┘
                             │
                             ▼
                  ┌──────────────────────┐
                  │ Return nextSendAt    │
                  │ (Firestore Timestamp)│
                  └──────────────────────┘
```

---

## 6. Deployment Architecture

```
┌──────────────────────────────────────┐
│         Development Machine          │
│                                      │
│  $ npm run deploy                    │
│  $ firebase deploy --only functions  │
└──────────────────┬───────────────────┘
                   │
        ┌──────────┼──────────┐
        │          │          │
        ▼          ▼          ▼
┌──────────────┐ ┌────────────────┐ ┌─────────────┐
│  Firebase    │ │ Cloud Storage  │ │ Cloud Build │
│  Functions   │ │ (artifacts)    │ │ (if config) │
│              │ │                │ │             │
│ • whatsapp   │ │ • Code zips    │ │ • Triggers  │
│   Scheduler  │ │ • Dependencies │ │ • Logs      │
│              │ │                │ │             │
│ • WhatsApp   │ │                │ │             │
│   Service    │ │                │ │             │
└──────────────┘ └────────────────┘ └─────────────┘
        │                │                │
        └────────────────┼────────────────┘
                         │
        ┌────────────────▼────────────────┐
        │  Google Cloud Platform          │
        │  (us-central1 region)           │
        │                                 │
        │  ┌──────────────────────────┐   │
        │  │ Cloud Functions          │   │
        │  │  ✓ sendScheduledWhatsApp │   │
        │  │  ✓ initializeSchedule    │   │
        │  └──────────────────────────┘   │
        │           │                     │
        │           ▼                     │
        │  ┌──────────────────────────┐   │
        │  │ Cloud Scheduler          │   │
        │  │  ✓ send-whatsapp-reports │   │
        │  │    every 15 minutes      │   │
        │  └──────────────────────────┘   │
        │           │                     │
        │           ▼                     │
        │  ┌──────────────────────────┐   │
        │  │ Pub/Sub                  │   │
        │  │  ✓ send-whatsapp-reports │   │
        │  │    topic                 │   │
        │  └──────────────────────────┘   │
        │           │                     │
        │           ▼                     │
        │  ┌──────────────────────────┐   │
        │  │ Firestore                │   │
        │  │ ✓ whatsappSchedules      │   │
        │  │ ✓ reports (read)         │   │
        │  │ ✓ users (read)           │   │
        │  └──────────────────────────┘   │
        │                                 │
        └─────────────────────────────────┘
                         │
        ┌────────────────▼────────────────┐
        │  External Services              │
        │                                 │
        │  ┌──────────────────────────┐   │
        │  │ Twilio WhatsApp          │   │
        │  │ REST API                 │   │
        │  │ Sends messages           │   │
        │  └──────────────────────────┘   │
        │                                 │
        └─────────────────────────────────┘
```

---

## 7. Error Handling Flow

```
┌──────────────────────────────────┐
│ sendScheduledWhatsAppReports()   │
│ (Cloud Function)                 │
└──────────────┬───────────────────┘
               │
               ▼
        ┌──────────────────┐
        │ Try to query     │
        │ schedules        │
        └──┬───────────┬───┘
           │           │
        ┌──▼┐      ┌───▼────┐
        │✓  │      │✗ ERROR │
        └───┘      └───┬────┘
           │           │
           ▼           ▼
        Process    Log error
        each       & exit
        schedule
           │
           ▼
        ┌──────────────────┐
        │ Generate report  │
        │ for field        │
        └──┬───────────┬───┘
           │           │
        ┌──▼┐      ┌───▼────┐
        │✓  │      │✗ ERROR │
        └───┘      └───┬────┘
           │           │
           ▼           ▼
        Get report  Skip & log
        data           │
           │           ├─ "Report generation failed"
           │           │
           ▼           │
        ┌──────────────────┐
        │ Send WhatsApp    │
        │ message via      │
        │ Twilio           │
        └──┬───────────┬───┘
           │           │
        ┌──▼┐      ┌───▼────────────┐
        │✓  │      │✗ TWILIO ERROR  │
        └───┘      └────┬───────────┘
           │            │
           ▼            ├─ 401: Bad credentials
        Update          ├─ 400: Invalid number
        Firestore       ├─ 429: Rate limited
           │            ├─ 500: Server error
           │            │
           │            ▼
           │        Log error
           │        Continue loop
           │
           ▼
        ┌──────────────────┐
        │ ✅ Success       │
        │ Message sent     │
        └──────────────────┘
```

---

## 8. Firestore Query Optimization

```
┌────────────────────────────────────────┐
│  Query Pattern for Sending             │
└────────────────────────────────────────┘

Users Collection
├── Loop through each user
│   └── whatsappSchedules subcollection
│       │
│       └── Query: WHERE isActive == true
│                 AND   nextSendAt <= now
│                 AND   nextSendAt > (now - 15 minutes)
│
│       ┌─────────────────────────────────┐
│       │ Index Created Automatically:    │
│       │ - isActive (ascending)          │
│       │ - nextSendAt (ascending)        │
│       │ - Composite: isActive + time    │
│       └─────────────────────────────────┘
│
│       Returns: [schedule1, schedule2, ...]
│
│       For each result:
│       ├── Fetch corresponding report
│       ├── Get user phoneNumber
│       ├── Send WhatsApp
│       └── Update nextSendAt


Estimated Query Performance:
├── 1,000 users: ~500ms
├── 10,000 users: ~2-3s
└── 100,000 users: ~15-20s (within 60s timeout)

Read Cost:
├── Per execution: ~200-400 read operations
├── Per day (96 executions): ~20,000-40,000 reads
└── Monthly cost: $1-2 (free tier covers 50K/day)
```

---

## 9. Component Hierarchy

```
React Component Tree:

Dashboard
└── Settings Page (/dashboard/settings)
    └── WhatsAppScheduleManager
        ├── ScheduleForm
        │   ├── Parcelle Select Dropdown
        │   ├── Frequency Radio Button Group
        │   │   ├── Daily → (none)
        │   │   ├── Weekly → Day Selector
        │   │   ├── Monthly → Date Selector
        │   │   └── Custom → Advanced Options
        │   ├── Time Input (HH:mm)
        │   ├── Timezone Select Dropdown
        │   ├── Audio Checkbox
        │   ├── Charts Checkbox
        │   ├── Custom Message Input
        │   └── Submit Button
        │
        └── ScheduleList
            ├── Empty State (if no schedules)
            └── For each schedule:
                ├── Schedule Card
                │   ├── Field Name
                │   ├── Frequency Label
                │   ├── Next Send Time
                │   ├── Last Sent Time
                │   ├── Send Count
                │   ├── Edit Button
                │   ├── Delete Button
                │   └── Toggle Active (Power Icon)
                │
                └── Confirmation Dialog (on delete)
                    ├── "Delete schedule?"
                    ├── Cancel Button
                    └── Confirm Delete Button


Firestore Integration:
├── Load schedules on mount
├── Listen for real-time updates
├── Create schedule → instant Firestore write
├── Update schedule → merge Firestore updates
├── Delete schedule → remove from Firestore
└── Toggle active → update isActive field
```

---

These diagrams should help visualize how all components work together from user interaction through message delivery!
