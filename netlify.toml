[build]
  command = "npm run build && mkdir -p .netlify/static && cp -r .next/static/* .netlify/static/ 2>/dev/null || true"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
